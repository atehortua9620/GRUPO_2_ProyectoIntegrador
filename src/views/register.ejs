<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://kit.fontawesome.com/2946b0c24d.js" crossorigin="anonymous"></script>
    
    
    <link rel="stylesheet" href="/Estilos/register.css">
   <link rel="stylesheet" href="/Estilos/styles.css">
   <link rel="stylesheet" href="/Estilos/home.css">
   
</head>
<body>
    <%- include('./partials/menu',userlogged) %>
    <main>
        
        <form class="formulario-register" action="/users/register" method="POST" enctype="multipart/form-data">
            <h1>Please let us know about you</h1>
            
            <div class="columna" >
                <label>Name </label>
                <input class="inputs name" id="name" type="text" name="nombre" value="<%=locals.old ? old.nombre : null %>">
                <% if (locals.errors && errors.nombre ) { %>
                    <div class = "feedback" > <%= errors.nombre.msg%> </div>
                    <% } %>
            </div>
            <div class="columna" >
                <label>Nickname </label>
                <input class="inputs nick" id='nick'type="text" name="nickname" value="<%=locals.old ? old.nickname : null %>">
                <% if (locals.errors && errors.nickname ) { %>
                    <div class = "feedback" > <%= errors.nickname.msg%> </div>
                    <% } %>
            </div>
            <div class="columna" >
                <label>E-mail </label>
                <input class="inputs mail" id="mail" type="email" name="email" value="<%=locals.old ? old.email : null %>">
                <% if (locals.errors && errors.email ) { %>
                    <div class = "feedback" > <%= errors.email.msg%> </div>
                    <% } %>
            </div>
            <div class="columna" >
                <label>Country </label>
                <select class="inputs count" type="text" name="country" id="countrie">
                    <%for(let i in countries){%>
                    <option  value="<%=countries[i]%>"><%=countries[i].name%></option>
                    <%}%>
                </select>
                <% if (locals.errors && errors.country ) { %>
                    <div class = "feedback" > <%= errors.country.msg%> </div>
                    <% } %>
            </div>
            <div class="columna" >
                <label>Password </label>
                <input class="inputs pass" id="pass" type="password" name="password" value="<%=locals.old ? old.password : null %>">
                <% if (locals.errors && errors.password ) { %>
                    <div class = "feedback" > <%= errors.password.msg%> </div>
                    <% } %>
            </div>
            <div class="columna" >
            <label>Confirm your password </label>
                <input class="inputs confirm" type="password" name="forgot" id="confirm" value="<%=locals.old ? old.forgot : null %>">
                <% if (locals.errors && errors.forgot ) { %>
                    <div class = "feedback" > <%= errors.forgot.msg%> </div>
                    <% } %>
            </div>
            <div class="columna" >
                <label for="avatar">please load your avatar</label>
                <input accept="image/JPG,image/JPEG,image/PNG,image/GIF" class="inputs" type="file" name="avatar" id="ava">
                <% if (locals.errors && errors.avatar ) { %>
                    <div class = "feedback" > <%= errors.avatar.msg%> </div>
                    <% } %>
            </div>
            <div class="errores" >
                <ul></ul>
                </div>
            <div class="columna">
                <button type ="submit" class="botonSignIn">  SIGN-IN</button>
             </div>
            
        </form>
        <div class="boton-login">
            <a href="/users/login"><button  class="login">I HAVE AN ACCOUNT</button></a>
        </div>
    </main>
    <%- include('./partials/footer') %>
    <script src="/validacionesJs/registerValidator.js"></script>
    </body>
</html>