<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://kit.fontawesome.com/2946b0c24d.js" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="/Estilos/styles.css">
    <link rel="stylesheet" href="/Estilos/login.css">
    <link rel="stylesheet" href="/Estilos/home.css">
    
   
</head>
<body>
    <%- include('./partials/menu',userlogged) %>
    
        <main>
        <form class= 'formulario-login' action="/users/login" method="POST" >
            <h1>Login</h1>
            <div class="campos">
                <label>Mail</label>
                <input type="email" name="email" class="inputs" id="mail" value="<%=locals.old ? old.email : null %>">
                <% if (locals.errors && errors.email ) { %>
                    <div class = "feedback" > <%= errors.email.msg%> </div>
                    <% } %>
            </div>
            <div class="campos">
                <label>Password</label>
                <input type="password" name="password" class="inputs" id="pass" value="<%=locals.old ? old.password : null %>">
                <% if (locals.errors && errors.password ) { %>
                    <div class = "feedback" > <%= errors.password.msg%> </div>
                    <% } %>
            </div>
           <!--  <div class="campos">
            <label>Forgot your password</label>
                <input type="email" name="forgot" class="inputs">
            </div> -->
            <div class="errores" >
            <ul></ul>
            </div>
            <a href="/" class="login-cont"><button class="botonCreateAccount login">Log-in</button></a>
            </form>
            <div class="campos">
                <a href="/users/register" class="login-cont"><button class="botonCreateAccount login">I WOULD LIKE TO CREATE AN ACCOUNT!</button></a>
            </div>
    </main>
    <%- include('./partials/footer') %>
    <script src="/validacionesJs/loginValidator.js"></script>
    </body>
</html>